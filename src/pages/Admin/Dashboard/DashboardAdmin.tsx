import { FC, Fragment, memo } from "react";

const DashboardAdmin : FC = memo(() => {
    return (
        <Fragment>
            <div>Dashboard</div>
        </Fragment>
    )
})

export default DashboardAdmin;