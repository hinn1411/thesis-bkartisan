import { memo, FC } from 'react';

export interface ChangeAddressModalProps {}

const ChangeAddressModal: FC<ChangeAddressModalProps> = memo(() => {
  return <p>Modal</p>;
});

export default ChangeAddressModal;
