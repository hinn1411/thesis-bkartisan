import { FC, memo } from 'react';
import HeaderSeller from '../../../components/header/HeaderSeller';
const DetailProduct: FC = memo(() => {
  return (
    <HeaderSeller name = "Quản lý sản phẩm"></HeaderSeller>
  );
});

export default DetailProduct;
