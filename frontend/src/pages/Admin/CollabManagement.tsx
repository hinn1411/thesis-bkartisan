import { FC, Fragment, memo } from "react";

const CollabManagement : FC = memo(() => {
    return (
        <Fragment>
            <div>CollabManagement</div>
        </Fragment>
    )
})

export default CollabManagement;